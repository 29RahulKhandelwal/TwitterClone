extends layouts/LoginRegister-layout.pug
block content
    .container 
        h1 Login
        form(method="post")        
            p.errorMessage #{errorMessage}

            //- input(type="text", name="logUsername", placeholder="Username or email", value=logUsername required="")
            //- input(type="password", name="logPassword", placeholder="Password", required="")
            input(type="text", name="logUsername", placeholder="Username or email", value="ForProjectShowCase" required="")
            input(type="password", name="logPassword", placeholder="Password", value="123", required="")
            input(type="submit", value="Login")
        a(href="/register") Need an account? Register here.